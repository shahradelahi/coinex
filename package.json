{
  "name": "coinex-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "ci:publish": "changeset publish",
    "clean": "git clean -dfx node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "prettier": "@shahrad/prettier-config",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@shahrad/prettier-config": "^1.2.2",
    "@types/node": "^22.13.14",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=18",
    "pnpm": "10.x"
  }
}
